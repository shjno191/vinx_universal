{
  "name": "vinx-universal-monorepo",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/sdk",
    "packages/plugins/*"
  ],
  "scripts": {
    "dev": "npm run dev -w packages/core",
    "build": "npm run build --workspaces --if-present",
    "tauri": "npm run tauri -w packages/core",
    "test": "vitest"
  },
  "devDependencies": {
    "@vitest/ui": "^4.1.6",
    "typescript": "~5.6.2",
    "vitest": "^4.1.6"
  }
}
